# 미확정 사항 (Open questions)

규칙 11.9: 미확정 사항은 여기 남기고 진행을 막지 않는다. 확인되면 ADR 번호를 적고 행을 "완료"로 옮긴다.

| # | 항목 | 확인 방법 | 영향 | 담당 | 기한 | 상태 / ADR |
|---|---|---|---|---|---|---|
| 1 | asf_search burst 제품에서 픽셀 간격·IPF 버전·burst ID 필드 이름과 가용 시점 | `asf_search` 14.0.0 소스(`S1BurstProduct`) + 실제 응답 | SEL-08/09 | select | Phase 1 | 진행 중 → ADR-0010 |
| 2 | asf_search stack API의 Sentinel-1 수직 기선 계산 가능 여부 | 실제 호출 + 알려진 값 비교 (`-m network`) | SEL-06 | select | Phase 1 | 진행 중 → ADR-0011 |
| 3 | HyP3 크레딧 정책·작업별 비용 | HyP3 문서 | plan 견적 | engines/hyp3 | Phase 2 | 진행 중 → ADR-0020 |
| 4 | SNAPHU 라이선스 조건, snaphu-py/dolphin/spurt/sardem 저장소 LICENSE 원문 | LICENSE 파일 | §9 표 | Taylor | Phase 8 | 미착수 |
| 5 | topsStack 참조 기하 재사용 가능성 | ISCE2 소스 | PERF-11 | engines/isce2 | Phase 5 | 완료 → #45, ADR-0029 (동일 항목; 조사 결과는 #45 행) |
| 6 | 국토지리정보원 수준점·GNSS 데이터 접근 방식과 이용 조건 | 기관 안내 | Phase 4 어댑터 | 연구자 | Phase 4 | 미착수 |
| 7 | MintPy enu2los 부호·헤딩 규약 | MintPy 소스(`mintpy/utils/utils0.py`) | validate.los | validate | Phase 4 | 진행 중 → ADR-0040 |
| 8 | SNAPHU 메모리 상수(100 MB/백만 픽셀) 실측치 | bench S | unwrap 스케줄러 | bench | Phase 2 | 미착수 (설정값 `unwrap.memory_mb_per_mpixel`) |
| 9 | 타일 오버랩 기본값(25% vs 30% vs 200 px) | bench + 연구 모듈 지표 | unwrap 기본값 | research | Phase 6 | 미착수 (기본 25%, 최소 200 px) |
| 10 | 의존성 정책 예외 승인: `hyp3-sdk`(월 ~7.5k 다운로드), `sentineleof`, `sardem`, `snaphu`, `burst2safe`를 개발 환경에 설치할지 | Taylor 확인 | HyP3 경로·궤도·DEM 실행 테스트 | Taylor | Phase 2 | 대기 (ADR-0001) |
| 11 | zarr 3.x API 채택(설치된 3.1.6) vs 2.x 호환 | io 모듈 구현 시 확인 | PERF-08 | io | Phase 2 | 진행 중 → ADR-0050 |
| 12 | HyP3 RTC `_ls_map.tif`(uint8)의 비트/값 정의(레이오버·셰도우·유효 비트) | HyP3 문서 본문에 미기재 → ASF 문의 또는 hyp3-gamma 소스(`rtc_sentinel.py`) 확인 | ADR-0019 비교 인코딩 표 | select/geometry | Phase 2 | 미착수 (자체 ls_map은 ISCE2 인코딩 0/1/2/3 사용) |
| 13 | Sentinel-1 heading 사이트별 값: 기본값 −12°(상승)/192°(하강)는 중위도 근사. POEORB 상태벡터로 계산하는 `heading_from_orbit()` 필요 여부 | 실제 궤도 파일로 계산 후 MintPy `HEADING` 값과 비교(`-m network`) | ADR-0017, 기하 마스크 정확도(1~3°) | select/geometry + select/network | Phase 2 | 미착수 (기본값은 `S1_HEADING_ASC_DEG/S1_HEADING_DESC_DEG`, 인자로 덮어쓰기 가능) |
| 14 | 수동(passive) 레이오버·셰도우(레인지 방향 ray-cast) 자체 구현 필요 여부 및 `sardem` DEM을 ISCE2 topsStack 입력(`make_isce_xml`)으로 재사용할지 | ISCE2 설치 후 ADR-0019 프로토콜로 비율 차이 실측 | SEL-12 임계값, unwrap 마스크 | select/geometry | Phase 5 | 미착수 (ADR-0019 제안 상태) |
| 15 | `wintersar.diagnose.resources.estimate` 시그니처 — pipeline은 `estimate(stage=..., params=..., machine=...) -> Resources`로 호출하고 TypeError/미존재 시 빈 추정치로 폴백 | diagnose 모듈 구현 확인 | `plan` 리소스 표 | diagnose | Phase 3 | 대기 (pipeline/plan.py) |
| 16 | `wintersar.unwrap.api.run_unwrap(igrams: Artifact, params, out_dir, log_dir, machine) -> Artifacts` 반환형·retry_hint 전달 방식(예외 `retry_hint` 속성 또는 Finding `evidence.retry_hint`) | unwrap 모듈 구현 확인 | 실제 경로 unwrap 단계 | unwrap | Phase 2 | 대기 (ADR-0033/0034) |
| 17 | 파이썬 단계 진입점 이름: `wintersar.select.api.run_search/run_precheck`, `wintersar.validate.api.run_validate` — 시그니처 `fn(cfg, inputs, params, out_dir, log_dir) -> Artifacts \| (Artifacts, list[Finding])`; 없으면 단계를 건너뛰고 `PIPELINE-004` | select/validate 모듈 확인 | 실제 경로 search/precheck/validate | select, validate | Phase 1/4 | 대기 (pipeline/stages.py) |
| 18 | macOS `ru_maxrss` 단위(bytes) — man 페이지에 단위가 없어 로컬 측정치(빈 인터프리터 ≈ 13e6)로 판단, Linux는 KiB(man7 확인) | psutil `memory_info().rss`와 교차 확인 | manifest `resources.peak_rss_gb` | pipeline | Phase 2 | 대기 (executor._peak_rss_gb) |
| 19 | tophu `downsample_factor` 기본값: wintersar (3,3)(tophu 테스트값) vs dolphin (1,1) — 타일 경계 단차·속도 비교 | bench + research 지표 | tophu 기본값 | research | Phase 6 | 미착수 (ADR-0024 임시값 (3,3)) |
| 20 | SNAPHU 1.x 실행 파일의 `ASSEMBLEONLY dirname`(구 문법) 지원 여부 — 어댑터는 2.0 문법(`ASSEMBLEONLY TRUE`+`TILEDIR`)만 생성 | 1.x 설치 환경에서 실행 | assemble-only(PERF-03) | engines/snaphu | Phase 5 | 미착수 (ADR-0023, 지원 범위 `>=2.0,<3`) |
| 21 | snaphu-py 경로에서 타일 임시 파일 보존 불가(TILEDIR/RMTMPTILE 미노출) — 상류에 옵션 제안 여부 | snaphu-py 이슈/PR | 재튠 재사용 | engines/snaphu | Phase 5 | 미착수 (ADR-0023, 우회: backend=subprocess) |
| 22 | spurt conda-forge 패키지 부재(2026-09-16 404) 및 `pip install spurt`(ortools) 환경 정책 예외 승인 | Taylor 확인 | spurt 어댑터 실행 테스트 | Taylor | Phase 8 | 대기 (ADR-0025) |
| 23 | 프린지 밀도 임계값 `FRINGE_HIGH = 0.25`(tophu 우선 선택 기준)와 타일 시 워커 수를 코어까지 올리는 규칙의 실측 근거 | bench 합성 3종(가우시안·급경사 램프·강한 대기)에서 snaphu/tophu 오류율 교차점 + 타일 워커 스케일링 wall time | unwrap 자동 방법 선택·병렬도 | bench / research | Phase 6 | 미착수 (ADR-0045, ADR-0047) |
| 24 | looks 종횡비 상한 1.2(자동 looks 20 m → 4×1) vs HyP3 고정 옵션 5×1(θ=39°에서 종횡비 ≈1.31) 중 연구자 선호 | 연구자 확인 + 실데이터 간섭도 비교 | SEL-09 기본값, HyP3 어댑터 스냅 | 연구자 | Phase 1 | 대기 (ADR-0015) |
| 25 | SEL-07 계절 모델(북반구 11~3월 적설·낙엽, 5~9월 식생, 4·10월 환절기) 한국 사이트 적합성 및 SEL-12 임계 10% | 연구자 확인(Phase 1 규칙표 검수) | SEL-07/SEL-12 판정 | 연구자 | Phase 1 | 대기 (ADR-0014) |
| 26 | Earthdata **토큰만**(`auth_with_token`)으로 SLC BURST 추출기(`sentinel1-burst.asf.alaska.edu`) tiff/xml 다운로드가 되는지 (asf_search 문서는 `asf-urs` 쿠키 필요라고 명시, 실측 필요) | 실제 다운로드 1건 (`-m network`, 토큰만 설정) | fetch 단계·auth 정책(ADR-0013) | select | Phase 2 | 미착수 (fallback: .netrc 우선) |
| 27 | 검색 시점에 픽셀 간격이 없을 때 SEL-09/looks가 쓸 IW 명목값(2.329562 × 13.97~14.1 m, SentiWiki 2.3 × 14.1 m)의 출처를 리포트에 어떻게 표시할지 | looks/rules 담당과 합의 | SEL-09, looks 자동 산출 | select | Phase 1 | 미착수 (ADR-0010 참조) |
| 28 | #15·#16 후속: 트리 내 시그니처 확인 — `diagnose.resources.estimate(stage, n_pairs, pixels, engine, machine, params)`, `diagnose.api.diagnose_logs(path, engine=None, *, assume_failed=…)`, `unwrap.api.run_unwrap(igrams, params, out_dir, log_dir, machine) -> Artifacts`. pipeline은 이에 맞춰 호출하며(`# source:` 주석) 드리프트 시 빈 추정치/일반 Finding으로 폴백. #17의 select는 `select.api` 대신 `select.search`/`select.cli`/`select.report` 어댑터(`pipeline/stages.py`)로 연결, validate는 미착수 | 통합 테스트 (실제 경로) | plan 견적·실패 진단·search/precheck 단계 | pipeline | Phase 2 | 진행 중 |
| 29 | SNAPHU 구버전(1.4.2 등)의 secondary-node 초과 메시지 문구 — KB-SNAPHU-001 은 snaphu-py 가 vendoring 하는 gmgunter/snaphu(2.x) `snaphu_tile.c` 의 "Exceeded maximum number of secondary nodes" 만 검증했고, 플랜의 "secondary arcs" 문구는 소스에 없음 | 구버전 snaphu_tile.c 또는 실제 로그로 확인 후 패턴에 alternation 추가 | KB-SNAPHU-001 매칭(구버전 SNAPHU 사용자) | diagnose | Phase 3 | 진행 중 → ADR-0035 |
| 30 | HyP3 무료 크레딧(월 8,000)의 리셋 시점(매월 1일인지 가입일 기준인지) — credits 문서에 명시 없음 | HyP3 문서/ASF 문의 | plan 견적 문구·KB-HYP3-001 조치 | diagnose | Phase 3 | 미착수 (resources.yaml `reset_timing: null`) |
| 31 | ISCE2 topsStack 에서 DEM 이 bbox 를 덮지 못할 때 명시적 오류 문자열이 나는지 — KB-ISCE2-004 는 DemStitcher/runVerifyDEM 문자열만 사용하며 topo 단계가 조용히 null 을 낼 가능성 | S 사이트에서 일부러 작은 DEM 으로 topsStack 실행 후 로그 확보 | KB-ISCE2-004 재현율, 픽스처 추가 | diagnose | Phase 3 | 미착수 → ADR-0035 |
| 32 | `diagnose/resources.yaml` 의 시간·메모리·디스크 계수는 전부 초기 추정치(`status: initial_guess`) — 실측 회귀로 갱신 필요 | bench S 사이트, 픽셀 수 3개 이상 실측 → bench_result.json | plan 예상치, unwrap 스케줄러 타일 결정 | bench | Phase 5 | 미착수 → ADR-0036 |
| 33 | HyP3 작업이 FAILED 로 끝났을 때 실패 사유를 얻는 방법 — hyp3_sdk `Job` 에는 failure reason 필드가 없고 `status_code` 만 있음(제출 시 검증 오류만 HyP3Error 로 옴) | HyP3 API 응답 필드/작업 로그 다운로드 경로 확인 | 런타임 실패의 KB-HYP3 진단, engines/hyp3 어댑터 로그 형식 | engines/hyp3 | Phase 3 | 미착수 |
| 34 | HyP3 작업 `name` 길이·허용 문자 제한 (API 스키마 미확인) | HyP3 API 스키마(openapi) 또는 실제 제출 | hyp3 어댑터 작업 이름(`wintersar_<stack>_<pair>`, 40자 내외) | engines/hyp3 | Phase 2 | 미착수 (ADR-0020: 짧은 이름 유지) |
| 35 | `candidates.json` granule 계약: `StackCandidate.notes.granules = {date_iso: {burst_id: granule}}` 를 select가 기록 | select 모듈 구현 시 합의 | hyp3 어댑터 입력 (없으면 HYP3-007) | select | Phase 2 | 미착수 (ADR-0020) |
| 36 | `timeseries.coherence_threshold` → `mintpy.network.coherenceBased=yes + minCoherence` 매핑이 타당한지 (vs `networkInversion.minTempCoh`) | 연구자 확인 (규칙 11.10) | MintPy 네트워크 가지치기 기본 동작 | 연구자 | Phase 2 | 대기 (ADR-0021, 기본값 0.7) |
| 37 | HyP3 burst 산출물의 날짜별 지리 범위 편차(수 픽셀)와 공통 범위 클립(`*_clip.tif`)의 실데이터 검증 | S 사이트 실데이터 (`-m network`) | MintPy load_data 크기 불일치 | engines/hyp3 | Phase 2 DoD | 미착수 (ADR-0020 HYP3-009) |
| 38 | HyP3 `_conncomp.tif`를 `mintpy.load.connCompFile`로 함께 로드할 때 `prep_hyp3` 호환성 (MintPy docs dir_structure.md의 HyP3 예시는 unw/cor/dem/lv_theta/lv_phi/water_mask만 나열, cfg에서 connCompFile은 optional) | S 사이트 실데이터로 load_data 실행 (`-m network`) | MintPy unwrapError bridging 입력, HyP3 템플릿 패턴 | engines/hyp3 | Phase 2 DoD | 미착수 (ADR-0021; 실패 시 패턴에서 제외) |
| 39 | LOS 부호 규약(ADR-0040 표: 융기 +cos θ, 동향 상승 −/하강 +)을 실데이터(알려진 GNSS 지점의 상승·하강 LOS)로 연구자가 확인 | 국내 사이트 GNSS 1점 + 상승/하강 시계열 비교 | validate.los, 대조군 RMSE 부호 | 연구자 | Phase 4 DoD | 대기 (ADR-0040, 규칙 11.10) |
| 40 | 기준점 점수 가중치 초기값(coh 0.35 / conncomp 0.25 / elev 0.15 / dist 0.10 / vel 0.15)과 최소 분리 5 px의 타당성 | 국내 사이트 후보 표를 연구자가 검토 | `wintersar refpoint` 순위 | 연구자 | Phase 4 | 대기 (ADR-0041) |
| 41 | 폐합 의심 임계값(unw 비영 픽셀 5%, wrapped 0.3 rad + 3·MAD)과 MintPy `numTriNonzeroIntAmbiguity.h5` 대조 | MintPy 산출물을 h5py로 읽는 io.formats 후 동일 스택 비교 | closure 의심 간섭도 목록, sweep closure_rms | validate + io | Phase 5 | 미착수 (ADR-0042) |
| 42 | 국토지리정보원 수준점 **시계열**(재측량 성과) 입수 경로, GNSS 일 단위 좌표 시계열 산출 방법(RINEX 후처리 vs 제공), geodesy.ngii.go.kr / gnssdata.or.kr 이용약관 원문 | 기관 문의 + 약관 페이지 확인(로그인 필요) | NGII 어댑터 구현 여부(현재 CSV만) | 연구자 | Phase 4 DoD | 미착수 (ADR-0043) |
| 43 | sweep 대용 지표(`temporal_coherence` = 폐합 코히어런스, `residual_rms` = unw−시계열 차)를 MintPy `temporalCoherence.h5`·`timeseriesResidual.h5` 실제 값으로 교체 | io.formats 로더 구현 후 fake/실데이터 비교 | sweep Pareto 순위 | validate + io | Phase 5 | 미착수 (ADR-0044) |
| 44 | `wintersar.io.formats.load_timeseries(path) -> TimeSeries` 시그니처(MintPy timeseries.h5 + geometryGeo.h5 + HEADING 속성) — validate.api는 존재하면 위임, 없으면 .npz 폴백 | io 모듈 구현 확인 | `wintersar validate --ts timeseries.h5` | io | Phase 5 | 대기 (validate/api.py `_formats_loader`) |
| 45 | (완료) topsStack 참조 기하 재사용: `checkCurrentStatus`가 `coreg_secondarys/` 기준 업데이트 모드로 `unpack_topo_reference`·`extract_burst_overlaps`를 건너뜀; 최근 2·num_overlap_connections 날짜 원본 SAFE 필요 | stackSentinel.py 소스 | PERF-11 | engines/isce2 | Phase 5 | 완료 → ADR-0029 |
| 46 | ISCE2 미설치 상태에서 검증한 stackSentinel.py 플래그·run_files 이름·업데이트 모드 동작을 실제 설치 환경(S 사이트)에서 실행으로 재확인 | ISCE2 conda 환경에서 `wintersar run --until interferogram` (engine.interferogram=isce2_topsstack) | ADR-0026/0027/0029, 어댑터 실행 테스트 | engines/isce2 | Phase 5 | 미착수 |
| 47 | topsStack 이온층 단계의 순차 실행 권고 원문(README) 미재확인 — 현재 병렬 상한 1(보수적); `Stack.py`는 subband_and_resamp/generateIgram_ion에 numProcess 전달 | README 원문 확인 후 `DEFAULT_STEP_PARALLEL` 조정 | PERF-07 이온층 워크플로 속도 | engines/isce2 | Phase 5 | 미착수 (ADR-0027) |
| 48 | dolphin `worker_settings.n_workers`(작업 설명)는 main(0.42.7)에 없음 — `threads_per_worker`/`n_parallel_bursts`만 존재; 구버전 키 호환 범위와 `tophu` 언래핑 경로(UnwrapMethod에 tophu 없음, tophu_options만 존재)의 실제 동작 | dolphin 설치 후 `dolphin config --print-empty`로 스키마 덤프 비교 | dolphin 어댑터 YAML 생성, 버전 핀 `>=0.40,<1` | engines/dolphin | Phase 5 | 미착수 (ADR-0028) |
| 49 | dolphin 참조점을 lat/lon으로 받아 row/col로 바꾸는 변환(지오레퍼런스 래스터/레이더 좌표 lat.rdr) 및 `wintersar.io.formats.read_isce_raster`로 ISCE 평면 바이너리(`igrams_manifest.json`) 읽기 | io 모듈 구현 후 어댑터 연결 | PERF-05 A/B 비교, unwrap 단계의 ISCE 입력 | io + engines/dolphin | Phase 2 | 미착수 (ADR-0029) |
| 50 | `synth.okada_displacement` 훅은 `NotImplementedError` — Okada(1985) 직사각 전위원을 자체 구현할지, 검증된 외부 구현(예: `okada_wrapper`, 채택 기준 10k 사용자 미달 가능)을 쓸지 | 후보 패키지 PyPI 다운로드·라이선스 확인 후 ADR, 또는 Okada 1985 식 자체 구현 + 문헌 검산값 테스트 | 합성 변형원 종류(플랜 5.7 "선택적으로 Okada") | research | Phase 6 | 미착수 (ADR-0060 §Okada) |
| 51 | SHP 검정 기본값(KS, alpha 0.05, 창 = factor 이상 최소 홀수, 점근 임계값 `K_alpha·sqrt((n+m)/nm)`)이 실데이터(n_dates < 20)에서 보수적인지 — 점근 근사는 소표본에서 기각률이 alpha보다 낮음 | S 사이트 실 SLC 스택으로 SHP 가족 크기 분포·시간 코히어런스 비교, scipy `ks_2samp(method="exact")` 대비 선택률 | `research.repr_phase.shp` 기본값 | research | Phase 6 | 미착수 (ADR-0062) |
| 52 | `ml` 기준선은 tophu `do_lowpass_filter=False` 경로(단순 블록 평균)와 같음. tophu 기본값은 equiripple 저역통과(`shape_factor=1.5, overhang=0.5, ripple=0.01, attenuation=40`) 후 멀티룩 — 이 필터를 `ml_lowpass` 방법으로 추가해 비교할지 | tophu `_multiscale.py`/`_filter.py` 파라미터를 scipy.signal.remez 로 재현 후 합성 비교 | R-07 기준선 정의(ADR-0061), 오픈 항목 #19(downsample_factor) | research | Phase 6 | 미착수 (ADR-0060) |
| 53 | R-15 A/B(`S_synth_seq_estimator_ab`)는 `dolphin`·`mintpy` 설치가 필요 — dolphin(pip 가능, 채택 기준 확인 필요)과 MintPy(GPL-3, 서브프로세스 실행만) 설치 정책 예외 승인 | Taylor 확인(ADR-0001 정책) → 설치 후 실데이터 S 사이트 프로토콜(ADR-0064) 실행 | PERF-05/06 채택 판단 | Taylor / research | Phase 5~6 | 대기 (ADR-0064) |
| 54 | `stitching.coarse_ref`는 타일 단위 오프셋(tophu는 연결성분 단위 `adjust_conncomp_offset_cycles`) — 백엔드의 conncomp 라스터를 받아 성분 단위로 확장할지 | unwrap 실행기가 타일별 conncomp 를 stats 와 함께 넘기는지 확인 후 `stitch(..., conncomp=)` 추가 | 타일 안 다중 연결성분에서의 스티칭 정확도 | research + unwrap | Phase 6 | 미착수 (ADR-0063) |
| 55 | Goldstein 필터 구현 상수(패치 64, 오버랩 50%, 스펙트럼 3×3 평활, Hann 합성)가 Goldstein & Werner (1998, doi:10.1029/1998GL900033) 원 논문·ISCE2 `FilterModule`의 값과 같은지 | 논문 본문·isce2 소스 확인 후 기본값 조정, research 모듈 지표로 A/B | compute.kernels.goldstein_filter 기본값, engine.filter 설정 | compute | Phase 5 | 미착수 (ADR-0053) |
| 56 | Zarr `timeseries` 프리셋 `(all, 512, 512)`는 쌍 수 100+에서 청크 100 MB 이상 → 픽셀 조회가 청크 전체를 해제. `(all, 256, 256)`·zarr 3 `shards` 대안 실측 | `io.zarr_store.benchmark_chunk_presets`를 S 사이트 스택(실데이터)에 적용해 bench_result.json에 기록 | PERF-08 기본 프리셋 | io | Phase 2 DoD | 미착수 (ADR-0050) |
| 57 | ISCE2 BYTE 자료형 부호: `Image.py TO_NUMPY`는 `i1`, `renderVRT`/MintPy는 unsigned(Byte/uint8). wintersar는 uint8로 읽음 — 값 ≥ 128을 쓰는 사용자 파일이 있는지 | ISCE2 사용자 파일 사례 확인 | io.formats.read_isce_raster | io | Phase 5 | 미착수 (ADR-0052) |
| 58 | 벤치 회귀 게이트(15%)의 기준선을 어느 머신에서 만들지 — GitHub 호스티드 러너는 실행 간 변동이 커서 합성 S 사이트에서도 15%를 넘을 수 있음. 셀프호스트 러너 또는 반복 5회·임계 완화 검토 | CI에서 S_synthetic 10회 실행해 wall time 분산 측정 | benchmarks/baselines/*.json 생성 정책, CI 실패율 | bench | Phase 2 DoD | 미착수 (ADR-0054) |
| 59 | `pyproject.toml` `zarr>=2.18` → `zarr>=3.0` 상향 필요(`io.zarr_store`는 3.x 전용 API `create_array`/`ZstdCodec` 사용) 및 `engines/mintpy.read_timeseries_h5`를 `io.formats.read_timeseries_h5`로 위임 | 통합자 확인 | 의존성 해상도, 포맷 리더 단일화 | 통합자 | Phase 2 | 대기 (ADR-0050/0052) |
| 60 | Phase 7 DoD "QGIS LTR 2종에서 동작 확인": 이 환경에는 QGIS 가 없어 플러그인의 Qt 부분(`dock_widget.py` 의 `build()`·핸들러, Processing 클래스 팩토리)은 실행 검증하지 못함. 3.44 LTR(Qt5)과 4.2(Qt6, 2026-09-25 LTR 예정)에서 수동 검수 필요 | QGIS 3.44 + 4.2 에 `qgis_plugin/build_zip.py` 산출 ZIP 설치 후 6 패널·Processing 알고리즘 시연 | R-12, Phase 7 DoD | docs/qgis | Phase 7 | 미착수 (ADR-0070; 순수 파이썬 부분은 `tests/unit/qgis` 로 검증) |
| 61 | 플러그인이 환경의 파이썬을 직접 실행하면 conda 활성화 훅의 환경 변수(`PROJ_LIB`, `GDAL_DATA`, ISCE2 의 `ISCE_HOME`/`PATH` 추가분)가 적용되지 않음 — `conda run` 폴백이 필요한 엔진 목록 확인 | ISCE2·MintPy 가 설치된 conda 환경에서 플러그인으로 `run` 실행 | QGIS 경로의 로컬 엔진 실행 | docs/qgis | Phase 7 | 미착수 (ADR-0071; `conda:<name>` 힌트에 런처 우선 옵션 추가 가능) |
| 62 | QGIS 4.x 의 사용자 프로필 폴더 이름(`QGIS/QGIS3/profiles` 유지 여부) — 3.44 사용자 안내서만 확인, 4.x 문서 미확인 | QGIS 4.2 문서/설치본에서 `QgsApplication.qgisSettingsDirPath()` 값 확인 | `qgis_plugin/README.md` 설치 경로 안내 | docs/qgis | Phase 7 | 미착수 |
| 63 | `wintersar --json run` 은 마지막에 봉투 하나만 내므로 플러그인의 진행률은 "실행 중/완료" 수준 — 단계별 진행 이벤트(JSON lines `--progress`)를 CLI 에 추가할지 | pipeline 담당과 합의 | QGIS 패널 ③ 진행 표시 | pipeline / docs/qgis | Phase 7 | 미착수 (ADR-0070) |
| 64 | pixi 환경 디렉터리 레이아웃(`.pixi/envs/<env>/bin/python`)을 문서로 확인하지 못해 `pixi:` 힌트는 `pixi run --manifest-path` 런처만 사용 | pixi 문서(environments) 확인 후 직접 인터프리터 경로 추가 | 플러그인 환경 탐색 속도·런처 의존 | docs/qgis | Phase 7 | 완료 → ADR-0105 (환경 디렉터리 `.pixi/envs/<name>/{bin,conda-meta,...}` 확인; qgis `pixi:` 힌트에 인터프리터 경로 추가는 백로그) |
| 65 | `mkdocs.yml` nav 가 가리키는 `plan/wintersar_implementation_plan.md` · `research/index.md` 가 `docs/` 아래에 실재해야 `mkdocs build` 가 성공한다(행 작성 시점에는 `docs/plan/` 이 비어 있었고 `research/index.md` 도 없었음) | `ls docs/plan/ docs/research/index.md` | `mkdocs build` 성공 여부 | 통합자 | Phase 8 | 완료 (2026-09-17 확인: `docs/plan/wintersar_implementation_plan.md` 가 실제 파일로 존재하고 저장소 루트에는 사본이 없음, `docs/research/index.md` 도 존재 → 복사·심볼릭 링크 결정 불필요, ADR-0072) |
| 66 | 플랜 §9 라이선스 표의 미확인 행: COMPASS·isce3·LiCSBAS·tophu 라이선스와 사용 경계(LiCSBAS 는 알고리즘 참조만, 코드 복사 금지) — ADR-0001 은 asf-search·hyp3-sdk·sentineleof·snaphu/sardem 만 기록 | 각 저장소 LICENSE 원문 확인 후 ADR-0001 §9 표를 행별 상태와 함께 채움 | v0.1 배포 전 법적 경계(플랜 §9) | Taylor | Phase 8 | 완료 → ADR-0001 §9 표 (2026-09-17 각 저장소 LICENSE 원문 확인: COMPASS Apache-2.0, isce3 Apache-2.0 + EAR99 NLR 고지, LiCSBAS GPL-3.0, tophu BSD-3-Clause OR Apache-2.0). SNAPHU·snaphu-py·dolphin·spurt·sardem 는 #4 에 남아 있음 |
| 67 | Phase 0 / PERF-12 산출물 `pixi.toml`(또는 conda-lock) 락파일 부재 — `uv.lock` 은 conda 패키지(isce2·snaphu·tophu·mintpy)를 표현하지 못하고, 이 머신에는 conda/pixi 가 없어 검증할 수 없음(CLAUDE.md Environment) | 엔진 설치 승인(#10·#22·#53) 후 pixi 환경에서 생성·검증하거나, 엔진 환경은 튜토리얼로만 안내한다고 ADR 로 기록 | 플랜 §4.2 설치 경로, Phase 0 DoD | Taylor | Phase 8 | 진행 중 → ADR-0105/0106/0107 (pixi.toml·Dockerfile.engines 작성됨; 락파일 생성은 #70) |
| 68 | 골든 통계(ADR-0100)는 macOS arm64 에서 생성해 Linux x86_64 CI 에서 비교 — 플랫폼 간 float32 마지막 자리 차이가 ADR-0102 허용 오차(rtol 1e-6, `*_fraction` atol 1e-4) 안에 드는지 미실측. 실데이터 S 사이트 골든(플랜 §8 "S 사이트")은 네트워크·엔진 필요로 미포함 | 첫 nightly 실행(`.github/workflows/nightly.yml` regression 잡)의 `golden-check` 아티팩트에서 GOLDEN-001 evidence 확인; 노이즈면 rtol 1e-5 로 갱신 | `tests/regression/test_golden_s_synthetic.py`, `scripts/check_golden.py` 의 CI 안정성 | bench | Phase 8 | 미착수 (ADR-0100/0102) |
| 69 | pixi `[feature.isce2.activation.env] PATH = "$CONDA_PREFIX/share/isce2/topsStack:$PATH"` — 매니페스트 레퍼런스는 `env` 값이 기존 변수를 참조할 수 있다고만 하고, 패키지 `activate.d` 스크립트·`scripts`·`env` 의 적용 순서와 확장 시점의 `CONDA_PREFIX` 가용성은 적지 않음(conda-forge isce2 는 topsStack 을 PATH 에 올리지 않음) | pixi 설치 머신에서 `pixi run -e engines which stackSentinel.py` + `wintersar check-install --engine isce2_topsstack` | 로컬 ISCE2 경로 감지(ENV-001) | Taylor / engines/isce2 | Phase 2 | 미착수 (ADR-0105; 폴백: Dockerfile.engines 의 `ENV PATH`, docs/install.md 의 수동 export) |
| 70 | `pixi.lock` 부재 — `pixi.toml` 은 문서·레지스트리로만 검증했고 이 머신에는 pixi 가 없어 해상·락 생성을 못 함. noarch 엔진(mintpy, dolphin)의 전이 의존성이 osx-64/osx-arm64 에서 실제로 풀리는지도 미확인 | pixi 가 있는 머신에서 `pixi lock`(또는 `pixi install -e engines`) 실행 → 생성된 `pixi.lock` 커밋, `Dockerfile.engines` 를 `--build-arg PIXI_INSTALL_FLAGS=--locked` 로 전환, `.gitignore` 에 `.pixi/` 추가 | PERF-12 재현성, Phase 0 DoD, #67 후속 | Taylor | Phase 8 | 미착수 (ADR-0105/0106/0107; #67 의 구현 단계) |
| 71 | Docker engines 이미지의 CI 빌드 여부 — 현재는 빌드 비용·라이선스 격리·락파일 부재로 CI 제외(ADR-0106). 락파일 커밋 뒤 수동 dispatch/주기 워크플로 추가 검토, 레지스트리 게시는 라이선스 검토(#4) 전제 | 락파일 커밋 후 로컬 `docker build -f Dockerfile.engines` 1회 성공 + 빌드 시간 `bench_result.json` 기록 | Phase 0 DoD "Docker 빌드"(engines 변형) | Taylor / 통합자 | Phase 8 | 미착수 (ADR-0106) |
| 72 | PERF-10 GPU 패리티 테스트(`tests/unit/compute/test_gpu_parity.py`, 커널 8종 + 이식 함수 9종)와 `bench_kernels` 의 GPU 경로를 CUDA 머신에서 실행하지 못함(이 머신에는 CuPy 없음) — ADR-0096 의 GPU 대 CPU 허용 오차(float32 1e-5 / 1e-4 rad, float64 1e-9, 불리언 마스크 불일치 ≤ 1e-3)는 잠정값 | CuPy(`wintersar[gpu]`)가 설치된 CUDA 머신에서 `pytest tests/unit/compute -m gpu` 실행, 필요 시 ADR-0096 표의 오차를 실측값으로 갱신하고 `wintersar bench` 로 bench_result.json 기록 | PERF-10 DoD(GPU 커널 CPU 일치 테스트), Phase 5 | compute | Phase 5 | 미착수 (ADR-0095/0096/0097; CPU 경로·강등 경로는 CI 에서 검증됨) |
| 73 | `mkdocs build` 미실행 — docs extra(`mkdocs`, `mkdocs-material`)가 개발 venv 에 설치되어 있지 않아 사이트 빌드·nav 렌더링·`README.md`/`index.md` 충돌 경고를 이 환경에서 확인하지 못함. 현재는 `tests/unit/qgis/test_docs.py` 가 nav 항목 존재·상대 링크·명령 유효성만 검사(ADR-0112) | `uv sync --extra docs` 후 `mkdocs build --strict` 1회 실행, 통과하면 CI 에 추가 | 문서 사이트 빌드 성공 여부, Phase 8 DoD 문서 항목 | docs/qgis | Phase 8 | 완료 (2026-09-23: docs extra 설치 후 `mkdocs build --strict` 통과; `docs/kb/README.md`→`overview.md` 개명으로 링크 경고 해소; CI `docs` 잡 추가) |
| 74 | PERF-06 실 엔진 참여(ADR-0082): isce2_topsstack 업데이트 모드가 새 쌍만 담은 run_files 를 만드는지(ADR-0029 #46)와 어댑터가 `pairs_reused`/`pairs_computed` 회계를 보고하도록 바꿀 때 run file 부분집합 실행이 단계 간 의존(ADR-0027)을 깨지 않는지 — ISCE2 미설치라 검증 불가. hyp3 는 `jobs.json` 재개로 이미 다운로드된 쌍을 건너뛰지만 `_pairs_done` 을 읽지 않아 `PIPELINE-016` 이 난다 | ISCE2 conda 환경(S 사이트)에서 `wintersar run --incremental`(cli 소유자가 플래그 추가 후) 6→7 날짜 실행, run_files 목록·`merged/interferograms` 대조; hyp3 어댑터에 `_pairs_done` 인정 추가 | 실 경로 증분 처리 시간, `plan` 의 N cached/M new 표시 | engines/isce2 · engines/hyp3 | Phase 5 | 미착수 (ADR-0080/0082; fake 경로·`run_unwrap` 은 구현·테스트 완료) |
| 75 | PERF-06 before/after 측정(증분 1장 처리 시간 vs 전체 재처리) — 플랜 §6.1 표의 측정 지표이며 규칙 11.8 에 따라 `bench_result.json` 없이는 수치를 적지 않음. 합성 S 사이트로도 측정 가능하지만 bench 모듈에 "같은 workdir 에서 n_dates N→N+1" 시나리오가 없음 | bench 사이트 YAML 에 증분 시나리오(2회 실행, 두 번째만 측정) 추가 후 `bench_result.json` 기록 | 플랜 PERF-06 DoD(before/after), ADR-0080 결과 절 | bench | Phase 5 | 미착수 |
